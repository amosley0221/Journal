// ─── Journal+ runtime config ────────────────────────────────────────
// Fill these in with your Supabase project's URL and **anon/public** API key.
// Both are safe to ship in client code (the anon key has no admin privileges;
// row-level security in Supabase enforces per-user access).
//
// Find them at: https://app.supabase.com/project/<your-project>/settings/api
//
// On Render: edit this file in your repo and redeploy, OR set the values via a
// Render "build" step that writes this file from env vars.

window.JOURNAL_CONFIG = {
  SUPABASE_URL: 'https://exhwjoaferzvxzntueah.supabase.co',
  SUPABASE_ANON_KEY: 'sb_publishable_zNLm32glApYRo7WPivi2Uw_LlmYwiej',
};
